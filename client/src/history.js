import { createBrowserHistory } from 'history';

// it is responsible for maintaining control over the history object
// of the browser navigation. In our case for Programmatic navigation
// of Submit button

export default createBrowserHistory();